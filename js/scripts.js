

function beepBoop(number) {

  const digArray = [];

  for (let i = 0; i <= number; index++) {
    return i.toString()
  }
}

function beepBoop(number) {

  for (let i = 0; i <= number; index++) {
    return (i.toString().includes(1))
    digArray[i] = "Beep!";
  }
}
